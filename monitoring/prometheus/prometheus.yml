global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "vocalbaby-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["vocalbaby-api:8000"]
        labels:
          service: "vocalbaby"
          environment: "production"
